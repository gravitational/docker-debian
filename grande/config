SUITE=${SUITE:-$DEBIAN_VERSION}
MIRROR=${MIRROR:-http://deb.debian.org/debian/}
FLAVOUR=${FLAVOUR:-minimal}
TMPFS_SIZE=${TMPFS_SIZE:-1073741824}
BOOTSTRAP_INCLUDE=${BOOTSTRAP_INCLUDE:-ca-certificates,dumb-init,inetutils-ping,iproute2,less,locales,procps,sudo}
